/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
  basePath: '/patriciaeliseu.github.io',
  assetPrefix: '/patriciaeliseu.github.io/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
